# kwa-projekat

Projekat iz predmeta Klijentske Web Aplikacije

## Pokretanje

```sh
cp .env.example .env   # podesi pristupne podatke za bazu
docker compose up --build
```

Aplikacija je zatim dostupna na <http://localhost>.
